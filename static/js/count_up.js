$("#count1").counter({
autoStart: true, // true/false, default: true
duration: 100000, // milliseconds, default: 1500
countFrom: 0,// start counting at this number, default: 0
countTo: 735,// count to this number, default: 0
runOnce: true,// only run the counter once, default: false
placeholder: "?", // replace the number with this before counting,
easing: "easeOutCubic", // easing effects
onStart: function() {}, // callback on start of the counting
onComplete: function() {}, // callback on completion of the counting
numberFormatter: // function used to format the displayed numbers.
    function(integer) {
      return parseInt (integer);
    }
});

$("#count2").counter({
autoStart: true, // true/false, default: true
duration: 100000, // milliseconds, default: 1500
countFrom: 0,// start counting at this number, default: 0
countTo: 1600,// count to this number, default: 0
runOnce: true,// only run the counter once, default: false
placeholder: "?", // replace the number with this before counting,
easing: "easeOutCubic", // easing effects
onStart: function() {}, // callback on start of the counting
onComplete: function() {}, // callback on completion of the counting
numberFormatter: // function used to format the displayed numbers.
    function(integer) {
      return parseInt (integer);
    }
});

$("#count3").counter({
autoStart: true, // true/false, default: true
duration: 100000, // milliseconds, default: 1500
countFrom: 0,// start counting at this number, default: 0
countTo: 1452,// count to this number, default: 0
runOnce: true,// only run the counter once, default: false
placeholder: "?", // replace the number with this before counting,
easing: "easeOutCubic", // easing effects
onStart: function() {}, // callback on start of the counting
onComplete: function() {}, // callback on completion of the counting
numberFormatter: // function used to format the displayed numbers.
    function(integer) {
      return parseInt (integer);
    }
});

$("#count4").counter({
autoStart: true, // true/false, default: true
duration: 100000, // milliseconds, default: 1500
countFrom: 0,// start counting at this number, default: 0
countTo: 4325,// count to this number, default: 0
runOnce: true,// only run the counter once, default: false
placeholder: "?", // replace the number with this before counting,
easing: "easeOutCubic", // easing effects
onStart: function() {}, // callback on start of the counting
onComplete: function() {}, // callback on completion of the counting
numberFormatter: // function used to format the displayed numbers.
    function(integer) {
      return parseInt (integer);
    }
});